# Development dependencies for Cor Gaze Detection Library
# Install with: pip install -r requirements-dev.txt

# Core runtime dependencies (Pure Python implementation)
numpy>=1.19.0
opencv-python>=4.5.0
matplotlib>=3.3.0
Pillow>=8.0.0

# Development and build tools (for optional C extension)
setuptools>=45.0.0
wheel>=0.36.0
# Note: C++ compilation tools only needed for optional C extension
# Cython>=0.29.0  # Optional for C extension
# pybind11>=2.6.0  # Optional for C extension

# Testing frameworks
pytest>=6.0.0
pytest-cov>=2.10.0
pytest-benchmark>=3.4.0
unittest-xml-reporting>=3.0.0

# Code quality tools
flake8>=3.8.0
black>=21.0.0
isort>=5.0.0
mypy>=0.800
pylint>=2.6.0

# Documentation tools
sphinx>=3.5.0
sphinx-rtd-theme>=0.5.0
sphinx-autodoc-typehints>=1.11.0
myst-parser>=0.15.0

# Performance profiling
cProfile
line_profiler>=3.3.0
memory_profiler>=0.58.0

# Build and packaging
build>=0.7.0
twine>=3.4.0
check-manifest>=0.46

# Pure Python Implementation Notes:
# - No OpenCV development headers required
# - No C++ compilation needed
# - Works with standard opencv-python package
# - Cross-platform compatibility guaranteed

# Optional enhanced OpenCV (includes additional algorithms)
# opencv-contrib-python>=4.5.0  # Uncomment for additional OpenCV features

# Development utilities
ipython>=7.20.0
jupyter>=1.0.0
notebook>=6.2.0